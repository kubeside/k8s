apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
    # Issuers
    - ./issuer.yaml
    # Secrets
    - ./secrets/domeneshop.sops.yaml

    # Certificates
    #- ./wildcard.yaml
    #- ./ctf-wildcard.yaml

