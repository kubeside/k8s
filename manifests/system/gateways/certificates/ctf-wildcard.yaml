apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ctf-wildcard-certificate
  namespace: gateways-system
spec:
  secretName: ctf-wildcard-certificate
  issuerRef:
    name: letsencrypt 
    kind: ClusterIssuer
  dnsNames:
  - "*.nerd.imavgs.no"
