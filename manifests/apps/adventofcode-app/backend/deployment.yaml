apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: adventofcode-app
  annotations:
    secret.reloader.stakater.com/reload: backend-config
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: ghcr.io/kubeside/images/adventofcode-backend@sha256:9ab99e34084629d4d422cf413a94f14e2b725085de898ac1f8a5164b7f8bae33
          ports:
            - containerPort: 8000
          volumeMounts:
            - name: config
              mountPath: /opt/aoc/config.json
              subPath: config.json
              readOnly: true
          env:
            - name: "RUST_LOG"
              value: "DEBUG"
      volumes:
        - name: config
          secret:
            secretName: backend-config
            items:
              - key: config.json
                path: config.json
        
        
