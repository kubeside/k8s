apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: netboot
  namespace: netboot-app
spec:
  blocks:
    - cidr: 10.4.0.52/32
  serviceSelector:
    matchLabels:
      io.kubernetes.service.namespace: "netboot-app"

