apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb
  namespace: metallb 
spec:
  releaseName: metallb
  interval: 10m
  targetNamespace: metallb
  chart:
    spec:
      chart: metallb
      sourceRef:
        kind: HelmRepository
        name: bitnami-helm
      version: 6.4.16
  values: {}
